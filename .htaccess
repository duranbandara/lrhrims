<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /lrhrims/

    # Route everything through public/
    RewriteCond %{REQUEST_URI} !^/lrhrims/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
